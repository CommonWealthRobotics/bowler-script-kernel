package junit.bowler;

import static org.junit.Assert.*;

import org.junit.Test;

public class JsonTester {

	@Test
	public void test() throws Exception {
		/*
		 * try{ ScriptingEngine.setupAnyonmous(); //ScriptingEngine.setAutoupdate(true);
		 * }catch (Exception ex){ com.neuronrobotics.sdk.common.Log.
		 * error("User not logged in, test can not run"); } File f = ScriptingEngine
		 * .fileFromGit(
		 * "https://github.com/madhephaestus/BowlerStudioExampleRobots.git",// git repo,
		 * change this if you fork this demo "exampleRobots.json"// File from within the
		 * Git repo ); com.neuronrobotics.sdk.common.Log.error("File: "+f);
		 * HashMap<String,HashMap<String,Object>> map = (HashMap<String, HashMap<String,
		 * Object>>) ScriptingEngine .inlineFileScriptRun(f, null); for(String
		 * menuTitle:map.keySet()){ HashMap<String,Object> script = map.get(menuTitle);
		 * com.neuronrobotics.sdk.common.Log.error((String)script.get("scriptGit"));
		 * com.neuronrobotics.sdk.common.Log.error((String)script.get("scriptFile")); ;
		 *
		 * }
		 */
	}

}
